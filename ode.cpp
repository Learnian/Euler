#include <vector>
#include <fstream>
#include <iostream>
#include <string>
#include <sstream>
using namespace std;

int main()
{

string dt;
string N;
ifstream  ("params.dat");
double dt=0.5;
vector<double> t = {0};
t.push_back(t[0]+dt);
cout <<t[1];
getline(dat,str2);
stringstream(str2)>>dot2;
stringstream(str)>>dot;
